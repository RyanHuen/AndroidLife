<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.chaozhuo.rxexample">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".base.BaseImplActivity" />
        <activity android:name=".base.JustFromActivity" />
        <activity android:name=".base.UnComDefiActivity" />
        <activity android:name=".keyword.MapKeyActivity" />
        <activity android:name=".keyword.flatmap.FlatMapKeyActivity"></activity>
    </application>

</manifest>